apiVersion: v1
kind: Service
metadata:
  name: pestapora-web
  namespace: pestapora
spec:
  type: LoadBalancer
  selector:
    app: pestapora-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
