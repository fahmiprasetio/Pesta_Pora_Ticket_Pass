# Reference only. DO NOT commit real keys.
# Prefer creating the secret imperatively (see k8s/README.md):
#   kubectl -n pestapora create secret generic pestapora-secret --from-literal=...
apiVersion: v1
kind: Secret
metadata:
  name: pestapora-secret
  namespace: pestapora
type: Opaque
stringData:
  SUPABASE_SERVICE_ROLE_KEY: "REPLACE_ME"
  MIDTRANS_SERVER_KEY: "REPLACE_ME"
  ADMIN_RESET_TOKEN: "REPLACE_ME"
