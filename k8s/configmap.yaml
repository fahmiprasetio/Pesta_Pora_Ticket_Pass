apiVersion: v1
kind: ConfigMap
metadata:
  name: pestapora-config
  namespace: pestapora
data:
  # Non-secret runtime config. Use "simulasi" for the load-test / HPA demo.
  NEXT_PUBLIC_PAYMENT_MODE: "simulasi"
  MIDTRANS_IS_PRODUCTION: "false"
  NEXT_PUBLIC_MIDTRANS_IS_PRODUCTION: "false"
