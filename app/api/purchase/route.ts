// TODO: POST proses beli, panggil RPC atomik purchase_ticket
import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json({ message: "TODO: POST proses beli, panggil RPC atomik purchase_ticket" });
}
