// TODO: GET info produk + sisa stok dari Supabase
import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ message: "TODO: GET info produk + sisa stok dari Supabase" });
}
