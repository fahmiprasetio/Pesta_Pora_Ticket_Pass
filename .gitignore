# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
next-env.d.ts

# Editor settings
.vscode/

# Agents & IDE config
.agents/
AGENTS.md
CLAUDE.md
skills-lock.json

# Supabase local dev (migrations already applied to cloud, not needed in repo)
supabase/

# Docker & Kubernetes deployment configs (ops only, not needed for web)
Dockerfile
.dockerignore
docker-compose.yml
k8s/

# Load testing (dev tool, not needed for web)
loadtest/

# Documentation (not needed for web)
README.md

# Default Next.js placeholder assets (unused)
/public/file.svg
/public/globe.svg
/public/next.svg
/public/vercel.svg
/public/window.svg

# k6 load test generated output (auto-generated, not source)
loadtest/summary.json
